/**
 * Minimum wallet balance (in ₹) a user must maintain to use paid actions:
 * posting a requirement, posting an available vehicle, or buying a plan.
 * This is a gate only — the balance is NOT deducted.
 */
export const MIN_WALLET_BALANCE = 500;
