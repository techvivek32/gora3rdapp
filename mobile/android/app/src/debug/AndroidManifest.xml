<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development (hot reload, etc.). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- DEBUG ONLY: allow cleartext HTTP so a debug build can reach the local dev
         backend (http://10.0.2.2:7001 from the Android emulator). Release builds
         are unaffected (HTTPS-only). -->
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic" />
</manifest>
